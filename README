ethos -- 32-bit operating system


bin/          -- binaries & disk images
disk/         -- files to be copied to the disk image on build

src/arch/i386 -- code bound to i686 architecture
src/drivers   -- various hardware drivers
src/kernel    -- core kernel functions (& basic cli for testing)
src/libc      -- kernel "libc" implementation
