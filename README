ethos - homebrew x86 os kernel


done:
	interrupts
	keyboard
	ATA/IDE (still need to switch from PIO to MMIO)
	basic fat32
	some libc/stdio stuff

to do:
	malloc, calloc, realloc 
	paging
	memory management in general beyond segmentation
	programs/tasks/ABI
	actual shell
	virtual 8086 mode (primarily for switching graphics modes without writing card-specific drivers)
	audio?
	
